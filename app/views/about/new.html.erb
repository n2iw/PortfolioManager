<% @title = 'Create a New Work' %>
<h1>New Work</h1>
<%= form_for( @work, :url => {action: :create}, :html => { :multipart => true }) do |f| %>
  <%= render partial: 'form', locals: {f: f} %>
<% end %>

<% @title = @work.name %>
<div class="content">
    <h1><%= @work.name %></h1>
    <p><%= @work.description %></p>

    <div class="social-buttons">
      <%# Facebook buttons begin %>
      <div class="fb-like" data-href="<%= request.original_url %>" data-width="200" data-layout="button_count" data-action="like" data-show-faces="true" data-share="true"></div>
      <%# Facebook buttons end %>
    </div>

    <%= render partial: 'process_links' %>

    <% @pictures.each do |p| %>
      <%= image_tag p.file.url(:large), alt: 'Picture' %>
    <% end %>

    <%= render partial: 'process_links' %>
    
    <div class="social-buttons">
      <%# Facebook buttons begin %>
      <div class="fb-like" data-href="<%= request.original_url %>" data-width="200" data-layout="button_count" data-action="like" data-show-faces="true" data-share="true"></div>
      <%# Facebook buttons end %>
    </div>
</div>

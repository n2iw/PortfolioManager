<div class="content">
    <h1><%= @work.name %></h1>
    <p><%= @work.description %></p>
    <% @pictures.each do |p| %>
      <%= image_tag p.file.url(:large), alt: 'Picture' %>
    <% end %>
</div>
